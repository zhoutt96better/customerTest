version: 0.1             
component: build
timeoutInSeconds: 1000
shell: bash           

exportedVariables:
  - IMAGEN_EDITADA
    
steps:
  - type: Command
    name: "Prueba"
    command: |
      echo "la version de python es:"
      python --version
      
outputArtifacts:
  - name: ${IMAGEN_EDITADA}
    type: BINARY
    location: todos_logos/${IMAGEN_EDITADA}.png